# Test extract without a previous response (should fail gracefully)
extract jsonpath "$.test" as $value
print "Value: $value"

# Now make a request
GET "https://jsonplaceholder.typicode.com/posts/1"

# This should work
extract jsonpath "$.title" as $title
print "Title: $title"